fn main() {
    // 使用 bool 关键字声明一个布尔变量，取值有 true 和 false
    let checked:bool = true;
    println!("Checked {}", checked);

    // 字符：是字符串的基本组成，采用 UTF-8 作为底层编码（而不是常见的使用 ASCII 作为底层编码），包含数字、字母、Unicode 和其它特殊字符
    let c:char = 'R';
    println!("c {}", c);
}
